# Synthetic E-commerce Exercise (Cursor IDE friendly)

## Quick run (local)
1. Create a Python venv and install Faker:

```bash
python -m venv venv
source venv/bin/activate   # or venv\Scripts\activate on Windows
pip install faker